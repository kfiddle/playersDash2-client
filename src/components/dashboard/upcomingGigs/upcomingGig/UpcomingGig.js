import { Card, CardContent } from "@mui/material";

import styles from "./UpcomingGig.module.css";

const UpcomingGig = ({ title }) => {
  return (
    <Card>
      <CardContent>title</CardContent>
    </Card>
  );
};

export default UpcomingGig;
